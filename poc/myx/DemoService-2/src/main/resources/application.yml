server:
  port: 8080
  servlet:
    contextPath: /api

spring:
  jpa:
    hibernate:
      ddl-auto: create
      
  datasource:
    url: jdbc:mysql://localhost:3306/db_webservice
    username: webservice
    password: welcome1
      
security:
  oauth2:
    resource:
      token-info-uri: http://localhost:9000/api/oauth/check_token/